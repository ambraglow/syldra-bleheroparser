## Syldra BLEHero parser
Tool i used to plot RSSI values to  measure [Syldra](https://github.com/ambraglow/syldra)'s antenna performance.

Usage is as follows:
```bash
cargo run filename first_mac second_mac_address
```
and for plotting the data:
```bash
cargo run plot
```